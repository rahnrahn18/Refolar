<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <SurfaceView
        android:id="@+id/preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top Center Settings Container -->
    <LinearLayout
        android:id="@+id/settings_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_hex_translucent"
        android:padding="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btn_settings_toggle"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_preferences"
            android:tint="#FFFFFF" />

        <LinearLayout
            android:id="@+id/settings_expanded"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:visibility="gone">

            <!-- Resolution, Format, Silent icons here (placeholders) -->
            <ImageButton android:layout_width="32dp" android:layout_height="32dp" android:src="@android:drawable/ic_menu_crop" android:tint="#FFFFFF" android:layout_marginStart="8dp"/>
            <ImageButton android:layout_width="32dp" android:layout_height="32dp" android:src="@android:drawable/ic_menu_save" android:tint="#FFFFFF" android:layout_marginStart="8dp"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Side Bar (Thumb Reach - Right Side) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <ImageButton
            android:id="@+id/btn_flash"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_hex_translucent"
            android:src="@android:drawable/ic_menu_camera"
            android:tint="#FFFFFF"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:layout_marginBottom="16dp"/>

        <ImageButton
            android:id="@+id/btn_filter_toggle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_hex_translucent"
            android:src="@android:drawable/ic_menu_gallery"
            android:tint="#FFFFFF"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:layout_marginBottom="16dp"/>

        <ImageButton
            android:id="@+id/btn_beauty_toggle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_hex_translucent"
            android:src="@android:drawable/ic_menu_view"
            android:tint="#FFFFFF"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:layout_marginBottom="16dp"/>

        <ImageButton
            android:id="@+id/btn_portrait_toggle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_hex_translucent"
            android:src="@android:drawable/ic_menu_crop"
            android:tint="#FFFFFF"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:layout_marginBottom="16dp"/>
    </LinearLayout>

    <!-- Bottom Controls -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:paddingBottom="32dp"
        android:paddingTop="16dp">

        <ImageButton
            android:id="@+id/btn_gallery"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_toStartOf="@id/btn_shutter"
            android:layout_centerVertical="true"
            android:layout_marginEnd="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_gallery"
            android:tint="#FFFFFF" />

        <ImageButton
            android:id="@+id/btn_shutter"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_centerInParent="true"
            android:background="@drawable/bg_hex_translucent"
            android:src="@android:drawable/ic_menu_camera"
            android:tint="#FFFFFF"
            android:scaleType="fitCenter"
            android:padding="20dp"/>

        <ImageButton
            android:id="@+id/btn_switch_camera"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_toEndOf="@id/btn_shutter"
            android:layout_centerVertical="true"
            android:layout_marginStart="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_rotate"
            android:tint="#FFFFFF" />
    </RelativeLayout>

    <!-- Filter List (Hidden by default) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/filter_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/btn_shutter"
        android:layout_marginBottom="16dp"
        android:visibility="gone"/>

    <!-- Aperture Slider (Hidden by default) -->
    <LinearLayout
        android:id="@+id/aperture_control_layout"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/filter_list"
        android:orientation="vertical"
        android:visibility="gone"
        android:background="@drawable/bg_hex_translucent"
        android:padding="8dp">

        <TextView
            android:id="@+id/aperture_value_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="f/2.8"
            android:textColor="#FFFFFF"
            android:gravity="center"/>

        <com.google.android.material.slider.Slider
            android:id="@+id/aperture_slider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:valueFrom="0.0"
            android:valueTo="10.0"
            android:value="5.0"/>
    </LinearLayout>

</RelativeLayout>
